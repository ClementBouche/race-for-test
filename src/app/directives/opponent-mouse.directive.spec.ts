import { OpponentMouseDirective } from './opponent-mouse.directive';

describe('OpponentMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new OpponentMouseDirective();
    expect(directive).toBeTruthy();
  });
});
